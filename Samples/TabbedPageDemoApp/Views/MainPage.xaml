<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:TabbedPageDemoApp.Controls"
    xmlns:vm="clr-namespace:TabbedPageDemoApp.ViewModels"
    x:Class="TabbedPageDemoApp.Views.MainPage"
    x:DataType="vm:MainViewModel">

    <!--<NavigationPage Title="One" IconImageSource="{StaticResource TabOne}">
        <x:Arguments>
            <local:HomePage Title="One" />
        </x:Arguments>
    </NavigationPage>
    <NavigationPage Title="Two" IconImageSource="{StaticResource TabTwo}">
        <x:Arguments>
            <local:MainPage Title="Two" />
        </x:Arguments>
    </NavigationPage>
    <NavigationPage Title="Three" IconImageSource="{StaticResource TabThree}">
        <x:Arguments>
            <local:MainPage Title="Three" />
        </x:Arguments>
    </NavigationPage>
    <NavigationPage Title="Four" IconImageSource="{StaticResource TabFour}">
        <x:Arguments>
            <local:MainPage Title="Four" />
        </x:Arguments>
    </NavigationPage>-->
</TabbedPage>
